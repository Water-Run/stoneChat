stoneChat 是一个简单的 LLM Web Chat，目标环境是 Windows XP 和 IE6。

运行要求
--------

需要 PHP 5.4。启动脚本使用 PHP 自带的 Web Server，也就是 `php -S`。

Windows XP 请使用这个版本：
https://windows.php.net/downloads/releases/archives/php-5.4.45-Win32-VC9-x86.zip

还需要 stunnel。它负责在老系统上处理 HTTPS。

Windows XP 请使用这个版本：
https://www.stunnel.org/archive/5.x/stunnel-5.26-installer.exe

准备配置
--------

复制一份配置模板：

  CONF_SMP.INI -> CONF.ini

然后用记事本编辑 `CONF.ini`。配置文件里面已经写了说明，按里面的注释改即可。

重点要改的地方：

  [User Admin]
    管理员用户。可以控制是否允许网页编辑配置、允许哪些模型。

  [User Guest]
    普通用户。默认密码是 guestpass，请按需要修改。

  [Model XXX]
    模型权限名。用户的 allow_models 使用这里的名字。

  [Provider N]
    实际 API 配置。每个 Provider 用 model_id 指向一个 [Model XXX]。

没有使用的 Provider 可以保留，但要设为：

  active = 0

这样启动检查不会要求它必须有真实 API key。

启动
----

双击：

  RUN.cmd

如果 `CONF.ini` 不存在，`RUN.cmd` 会从 `CONF_SMP.INI` 复制一份。

启动脚本会先检查：

  PHP 是否可用
  CONF.ini 是否合法
  端口是否被占用
  stunnel.exe 是否存在
  cacert.pem 是否存在
  HISTORY 目录是否可写

检查通过后，浏览器访问：

  http://localhost:9999

注意
----

本项目没有安装程序，也没有卸载程序。

下载后直接在目录里运行 `RUN.cmd`。删除项目目录即可删除程序本体。

`CONF.ini` 是主要说明文档。修改用户、模型、Provider、API key，都从这个文件开始。

开源协议
--------

stoneChat 使用 WTFPL 协议。

项目地址：
https://github.com/Water-Run/stoneChat
